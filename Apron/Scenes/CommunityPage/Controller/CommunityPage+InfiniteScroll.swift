//
//  CommunityPage+InfiniteScroll.swift
//  Apron
//
//  Created by Akarys Turganbekuly on 21.05.2022.
//

import UIKit
import Models

extension CommunityPageViewController {
  func configureRecipes(items: [RecipeResponse]) {
//        guard let section = sections.firstIndex(where: { $0.section == .recipiesView }),
//              let row = sections[section].rows.firstIndex(where: { $0 == .recipiesView }),
//              let cell = mainView.cellForRow(at: .init(row: row, section: section)) as? CommunityRecipeCell else { return }
//
//        guard let section = sections.firstIndex(where: { $0.section == .recipiesView }) else { return }
//        let oldCount = self.recipes.count
//        self.recipes.append(contentsOf: items)
//        let newCount = self.recipes.count
//        let indexPaths = (oldCount..<newCount).map { IndexPath(row: $0, section: section)}
//        cell.recipesCollectionView.performBatchUpdates {
//            cell.recipesCollectionView.insertItems(at: indexPaths)
//        }
    }
}
